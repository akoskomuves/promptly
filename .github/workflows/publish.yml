name: Publish to npm

on:
  release:
    types: [published]
  workflow_dispatch:

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v5

      - uses: pnpm/action-setup@v5

      - uses: actions/setup-node@v5
        with:
          node-version: "24"

      - name: Install and build
        run: pnpm install --frozen-lockfile && pnpm build

      - name: Configure npm for OIDC
        run: |
          npm config set registry https://registry.npmjs.org/
          npm config delete //registry.npmjs.org/:_authToken || true

      - name: Update npm
        # Pin to the last 11.x — npm 12.0.0's global install is missing the
        # `sigstore` module, which breaks `npm publish --provenance`. 11.18.0
        # still supports OIDC trusted publishing.
        run: npm install -g npm@11.18.0

      - name: Publish shared
        working-directory: packages/shared
        run: |
          PKG_NAME=$(node -p "require('./package.json').name")
          PKG_VERSION=$(node -p "require('./package.json').version")
          if npm view "$PKG_NAME@$PKG_VERSION" version 2>/dev/null; then
            echo "Version $PKG_VERSION already published, skipping"
          else
            npm publish --access public --provenance
          fi

      - name: Publish mcp-server
        working-directory: packages/mcp-server
        run: |
          sed -i 's/"@getpromptly\/shared": "workspace:\*"/"@getpromptly\/shared": "^0.1.0"/' package.json
          PKG_NAME=$(node -p "require('./package.json').name")
          PKG_VERSION=$(node -p "require('./package.json').version")
          if npm view "$PKG_NAME@$PKG_VERSION" version 2>/dev/null; then
            echo "Version $PKG_VERSION already published, skipping"
          else
            npm publish --access public --provenance
          fi

      - name: Publish cli
        working-directory: packages/cli
        run: |
          sed -i 's/"@getpromptly\/shared": "workspace:\*"/"@getpromptly\/shared": "^0.1.0"/' package.json
          PKG_NAME=$(node -p "require('./package.json').name")
          PKG_VERSION=$(node -p "require('./package.json').version")
          if npm view "$PKG_NAME@$PKG_VERSION" version 2>/dev/null; then
            echo "Version $PKG_VERSION already published, skipping"
          else
            npm publish --access public --provenance
          fi
