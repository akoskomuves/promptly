// Single source for the CLI version string. Keep in sync with package.json
// when bumping (the publish workflow reads package.json; this constant is
// what `--version` and the install ping report).
export const CLI_VERSION = "0.2.6";
